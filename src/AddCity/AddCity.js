import React from 'react';
import {
    View, 
    Text,
    StyleSheet
} from 'react-native'

export default class AddCity extends Component {
    render() {
        retrun (
            <View>
                <Text> AddCity Component</Text>
            </View>
        )
    }
}