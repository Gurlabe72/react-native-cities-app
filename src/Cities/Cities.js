import React from 'react';
import {
    View, 
    Text,
    StyleSheet
} from 'react-native'

export default class Cities extends Component {
    render() {
        retrun (
            <View>
                <Text> Cities Component</Text>
            </View>
        )
    }
}