import React from 'react';
import {
    View, 
    Text,
    StyleSheet
} from 'react-native'

export default class City extends Component {
    render() {
        retrun (
            <View>
                <Text> City Component</Text>
            </View>
        )
    }
}